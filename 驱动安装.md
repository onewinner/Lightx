# 注意

对于shentong的aci如果不配置则无法程序无法正常运行

gbase和db2驱动不配置不影响正常使用，只是相应功能无法使用

linux编译的去掉了CGO所以无法调用shentong、gbase、db2，故不用装驱动

# 神通数据库

**window 环境配置**

```
# 配置 动态链接库
# 将go-driver/driver/shentong/go-aci/lib/win64/aci.dll 复制 到 C:/Windows/System32（推荐） 下
# 或 将 aci.dll 放在程序目录
```



**linux 环境配置**

```
# 将go-driver/driver/shentong/go-aci/lib/linux64/libaci.so 复制 到 /usr/lib64/ 下
# 或 配置 动态链接库
export LD_LIBRARY_PATH=$LD_LIBRARY_PATH:go-driver/driver/shentong/go-aci/lib/linux64/
```

# gbase8s-odbc-driver

```
# 下载 https://gbasedbt.com/dl/odbc/GBase8s_3.0-Win64-ODBC-Driver.zip

# 解压
# 使用管理员权限运行00注册ODBC_管理员权限运行.cmd，注册ODBC驱动

# 打开window odbc配置，添加GBase驱动
```

# DB2

```
运行ps1脚本setenv_db2.ps1临时添加环境变量
setenv_db2_permanent.ps1添加用户环境变量
```

